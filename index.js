
const data = require('./_data/data'),
      server = require('./_server/server'),
      normalize = require('./_neural/normalize')
    
      server.start()


      // normalize.getMinMaxValues(apple, function(result) {
      //   console.log(result)
      // })
 
